/**
 * The Reactor client SDK's public API.
 *
 * <p>The package is {@link org.jspecify.annotations.NullMarked}: every type here is non-null unless
 * it says otherwise with {@code @Nullable}. That is what lets Kotlin — including this SDK's own
 * facade — read the API as real nullable and non-nullable types rather than platform types.
 */
@NullMarked
package inc.reactor.sdk;

import org.jspecify.annotations.NullMarked;
